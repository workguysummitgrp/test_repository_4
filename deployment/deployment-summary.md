See outputs/ai-powered-customer-onboarding-portal/deployment/deployment-summary.md for full local deployment guide.

Quick start:
1. cp .env.example .env
2. Set OPENAI_API_KEY in .env
3. docker compose up --build -d
4. Open http://localhost:3000
